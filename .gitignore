# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, generated by 'go test -c'
*.test

# Output of the go toolchain
*.out

# Dependency directories (golang >= 1.11)
/vendor/

# Go module cache (if not in project root, usually in $GOPATH/pkg/mod)
# /go.mod
# /go.sum

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~
*.log
.DS_Store

# Compiled Go programs
# This ignores any executable output from 'go build' or 'go install'
# For Garnery, if you run 'go install', the binary usually goes into your GOPATH/bin
# or current directory if GOPATH is not set.
Garnery
